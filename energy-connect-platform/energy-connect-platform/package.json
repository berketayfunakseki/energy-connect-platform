{
  "name": "energy-connect-platform",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript/Node.js energy-device connectivity platform with PostgreSQL, reliability patterns, observability, React ops UI, Docker and AWS CDK.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "start:api": "node dist/apps/api/src/server.js",
    "start:worker": "node dist/apps/worker/src/worker.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
